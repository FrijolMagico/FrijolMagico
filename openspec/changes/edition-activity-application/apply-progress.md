# Cumulative Apply Progress — Edition Activity Registration

## Git recovery and Work Unit 3 closure

- The original `.git` directory was deleted after B1 implementation. A complete source snapshot was preserved outside the repository (timestamped preservation snapshot). An independent clone of remote `dev` (`bc3746c6`) reconstructed completed WU1, WU2, and WU3-A commits. Their Git trees match the original frozen review trees, but commit IDs changed: WU1 `5b7bf928`, WU2 `6a493515`, WU3-A `469305d7`. Previous review authority/receipts were not assumed transferred to the new Git metadata. The snapshot captured the then-current B1 progress artifact, not the earlier detailed WU1/WU2 log; earlier evidence remains in Engram and checked tasks.
- User explicitly authorized B1's `size:exception` up to 500 lines and its commit. B1 was committed as `e574ef39` (450 additions) on `feat/edition-activity-registration-03b-admin-update`; fresh native high-risk review `review-2fef3a5f7aeeb388` approved and its exact acknowledgement burned authority. Resilience warning `R4-001` was informational only.
- User authorized B2's commit and review. B2 was committed as `d645c631` (99 additions + 155 deletions = 254 changed lines) on `feat/edition-activity-registration-03c-admin-dialog-save`; fresh native high-risk review `review-b3b9e0d78634a5f0` approved and its exact acknowledgement burned authority. Resilience warning `R4-001` was informational only. The branch is clean; all six WU3 tasks are checked and the overall change is 15/40. No push, issue, PR, live migration, full-admin green, or browser interaction verification is claimed.
- The B1/B2 entries below describe their implementation state at the time and predate these commits/reviews. This recovery record supersedes their historical “no commit/review” wording.

## Work Unit 3 — slice B1 update aggregate — completed within approved exception

- Consumed the supplied native `gentle-ai.sdd-status` v2 for `edition-activity-application`: `artifactStore: openspec`, `applyState: ready`, `nextRecommended: apply`, initially 9/40 tasks complete, no dependencies/blockers, repo-local workspace and allowed edit root (the then-current checkout). `actionContext` warnings: none. Strict TDD active (`openspec/config.yaml`, runner `bun run test`). `skill_resolution: paths-injected`; all six injected skills were read. Confirmed the authorized clean branch `feat/edition-activity-registration-03b-admin-update` before source edits.
- Scope was only B1: atomic update action + action tests. User expressly accepted `size:exception` for B1 only, with a hard maximum of 500 authored additions + deletions. Final authored diff was **450 additions, 0 deletions** (194 action + 256 test lines), under the approved ceiling by 50. No dialog or old action was changed. `updateParticipationAction` remained for the exhibition dialog.
- Added `updateActivityAggregateAction` to validate edition participation/activity IDs, verify ownership within one transaction, resolve the authoritative effective activity type (band is forced through the `musica` slug), normalize and convert optional Chile-local registration input, update entity/activity rows, upsert activity detail and complete registration by their unique keys, delete absent registrations (including eligible-to-music changes), and invalidate local/public tags only after commit. Complete registration with effective music type rejects before mutation. Database trigger defense remains in place; the action also explicitly clears the row.
- Added focused action tests for detail/registration upsert, absent config deletion, music rejection/cleanup, band normalization, edition/activity ownership mismatch, commit-only complete cache tags, and injected errors at participation, activity, detail, and registration mutation points. The harness models transaction commit/rollback at the action boundary; it is an injected DB mock, not a live/external database. No live migration or database access was used.
- **Strict TDD evidence (B1):** RED: focused test file first failed 0 pass / 4 failures because the action module did not exist. GREEN: after action implementation and test fixture/import corrections, focused file passed 5/5, 59 assertions. TRIANGULATE: participation/admin scoped suite passed 49/49, 245 assertions; each injected late failure kept commit marker false and skipped invalidation; success verified all required admin/public tags after commit. REFACTOR: Prettier, admin type-check and lint passed; corrected test/typing issues and removed unused import.
- **Verification (B1):** focused aggregate tests **5/5** (59 assertions); participation suite **49/49** (245 assertions); `bun run type-check --filter=@frijolmagico/admin`, `bun run lint --filter=@frijolmagico/admin`, and `git diff --check` passed at B1 completion. Full admin suite was not run. Older progress records pre-existing catalog/avatar failures and two `next/cache` load errors; not re-investigated or claimed fixed.
- **Files changed (B1):** new `apps/admin/src/app/(core)/eventos/participaciones/_actions/activities/update-activity-aggregate.action.ts` and `apps/admin/tests/unit/app/(core)/eventos/participaciones/_actions/update-activity-aggregate.test.ts`, plus tasks/apply-progress updates. No legacy action/dialog/exhibition caller changed.
- **Rollback boundary (B1):** remove the new aggregate action and test to revert B1; B2 dialog submission, WU1/WU2, and other callers remain independent. Delivery boundary: accepted B1 exception only, 450/500 lines; no commit/push/issue/PR/review/receipt/live migration. Historical environment note: final cleanup during B1 reported `.git` metadata missing; no recovery/reinitialization was attempted. B2 verified its own clean branch/worktree at start.
- **Persisted tasks at B1 completion:** 14/40 checked; each B1 task row was visibly `[x]`; parent-owned lifecycle rows remained unchanged.

## Work Unit 3 — slice B2 update dialog aggregate submission — completed

- Consumed the supplied native `gentle-ai.sdd-status` v2 for `edition-activity-application`: `artifactStore: openspec`, `applyState: ready`, `nextRecommended: apply`, repo-local workspace and allowed edit root (the then-current checkout), initially 14/40 tasks complete, no native blockers, and `actionContext.mode: repo-local`. Strict TDD is active (`openspec/config.yaml`, runner `bun run test`). `skill_resolution: paths-injected`; all six injected skills were read. Verified the working tree was clean on `feat/edition-activity-registration-03c-admin-dialog-save` before changes.
- Workload guard: the forecast says chained PR recommended and budget risk High. Parent assigned this concrete WU3 B2 work-unit slice on the current chain branch, so this implementation is limited to this slice. No `size:exception`; authored source/test diff remains within 400 lines. No WU4 files (read model, types, composer, form registration fields/defaults/presentation) were changed.
- Replaced only `UpdateActivityDialog`'s sequential `executeUpdatePlan`/separate participation, activity, and detail actions with one call to the reviewed `updateActivityAggregateAction`, passing current edition, participation, activity, and detail values. Preserved the existing form, accessibility markup, pending/disabled behavior, error/success feedback, form reset, dialog close, and destructive footer behavior. Left `updateParticipationAction` intact and verified the exhibition dialog remains its caller. No other legacy caller was removed.
- Updated the now-obsolete detail-persistence source contracts to assert aggregate submission and unconditional detail values; added focused dialog-save contract tests covering a single aggregate payload, removed per-record save calls, retained feedback/reset/close behavior, and exhibition caller preservation.
- **Strict TDD evidence (B2):**

| Behavior | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|
| Dialog uses a single aggregate save and preserves UX/caller boundaries | Added `update-activity-dialog.contract.test.ts` first; focused run failed (2 failures) because the dialog had no aggregate action and prior success/error checks did not yet match the intended result shape. | Replaced the sequential plan with the aggregate action call. Focused dialog + aggregate-action tests passed (8 tests total, 73 assertions); action harness confirmed transaction contract still works. | First participations-scope run surfaced four stale expectations in the pre-existing detail persistence contract, which asserted the now-retired branch-specific actions. Updated those tests to assert the new aggregate contract. Reran participations suite successfully: **50/50 tests, 255 assertions**. Admin type-check and lint passed. | Prettier formatted affected tests. Kept changes scoped to dialog submission and contract coverage; did not alter form fields/defaults or other callers. Final `git diff --check` passed. |

- **Verification:** `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones'` — **50/50 tests, 255 assertions**; `bun run type-check --filter=@frijolmagico/admin` — passed; `bun run lint --filter=@frijolmagico/admin` — passed. Runtime browser harness: **N/A**; this dialog's submission is covered by focused source contract tests plus the existing aggregate action transaction harness, and the initialized project config reports no Playwright/E2E setup. No live database/migration was run.
- **Files changed by B2:** `apps/admin/src/app/(core)/eventos/participaciones/_components/update-activity-dialog.tsx`; `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/update-activity-dialog.contract.test.ts` (new); `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/update-activity-dialog.contracts.test.ts` (revised obsolete save-path assertions); this progress file; `openspec/changes/edition-activity-application/tasks.md` (one WU3 implementation checkbox completed).
- **Persisted task state:** WU3 task rows are now complete across slices A, B1, and B2. Re-read `tasks.md` before return to confirm the B2 `GREEN: Remove or migrate...` row is visibly `[x]`. Overall task state advances from 14/40 to 15/40; no WU4/other work-unit or parent-owned rows were checked.
- **Review budget / chain boundary:** B2 only, bounded to one dialog save-path migration and its focused tests; authored source/test changes must remain <=400 additions + deletions. No commit, push, issue, PR, review/receipt, release/delivery gate, or live migration was performed.
- **Rollback boundary:** revert the dialog's `updateActivityAggregateAction` import/call and restore the three legacy action imports plus sequential save plan, and revert the two focused contract test files to their previous expectations. The aggregate action, WU1/WU2, WU3 A/B1, exhibition dialog and `updateParticipationAction`, and database migration are outside this slice's rollback boundary.

### Remaining exact unchecked implementation task lines

- [ ] RED: Add read-model and component tests for empty/update Chile-local defaults, registration join absence, music hiding/clearing, complete submission wiring, and forged payload coverage at the action boundary. <!-- sdd-owner: implementation -->
- [ ] GREEN: Left join registration in admin activity reads, propagate optional defaults/types/composer data, add registration fields, hide and clear them for music, and submit the aggregate action. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run admin tests and manually inspect the rendered form contract for create/update, canonical round trips, reset/refresh behavior, and no client-only eligibility trust. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Keep dialog changes limited to registration presentation and aggregate submission; remove duplicated state transformations and preserve existing form accessibility conventions. <!-- sdd-owner: implementation -->
- [ ] RED: Add tests for the exact left join, nested nullable registration object, absence of current-time SQL predicates, mapper/DTO propagation, missing-row `null`, and music-path exclusion. <!-- sdd-owner: implementation -->
- [ ] GREEN: Add the left join and registration JSON selection, extend public types/mappers/fixtures, and preserve `MusicActivityItem` routing without computing active state server-side. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run the scoped web tests and inspect generated/query fixtures to verify inactive registrations remain serialized and no browser database request is required. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Normalize nullable mapping and fixture helpers; avoid broad DTO or catalog/artist changes. <!-- sdd-owner: implementation -->
- [ ] RED: Test semantic anchor CTA variants, preserved top-bar/timeline labels and placement, no button nesting, visible keyboard focus, and readable default/hover/focus contrast; test reusable Badge as a non-interactive `span` accepting content without a tab stop. <!-- sdd-owner: implementation -->
- [ ] GREEN: Extract a reusable web link-CTA primitive with solid top-bar and offset-border timeline variants, reuse at both existing links, and add a reusable brand-aligned non-interactive web Badge; leave native `Button`, text-link `LinkBtn`, and admin Badge unchanged. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run scoped web tests and inspect focus/contrast and narrow visual parity for both existing links and the Badge; do not import admin Badge or use hardcoded `NewBadget`. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Share only navigation CTA visuals and retain existing web link semantics, labels, and layout without broad redesign. <!-- sdd-owner: implementation -->
- [ ] RED: Add pure helper and component tests for hidden server/first render, inclusive start/end, inactive/malformed values, boundary timers, timeout re-evaluation, focus/visibility reconciliation, cleanup, collapsed `Inscríbete` Badge, expanded-only `Inscríbete Aquí` anchor CTA, music exclusion, exact labels, target/rel, no button nesting, readable contrast, and focus-visible semantics. <!-- sdd-owner: implementation -->
- [ ] GREEN: Implement client-only leaf affordances with `active=false` initialization, boundary scheduling without polling, lifecycle listeners, safe anchor attributes through the shared link-CTA, reusable non-interactive web Badge at top-right, and CTA at the bottom of expanded content. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run web tests with fake clocks/timers and explicit focus/visibility events; verify server-rendered markup contains neither label and no CTA appears in summary/minimal cards. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Keep `ActivityItem` server-first, isolate timing logic from markup, and preserve existing card/details semantics and music rendering. <!-- sdd-owner: implementation -->
- [ ] Run the complete strict-TDD evidence pass: database, admin, and web RED/GREEN/TRIANGULATE/REFACTOR results are recorded in the apply/verify artifacts, with failures resolved before delivery. <!-- sdd-owner: implementation -->
- [ ] Run `bun run test`, `bun run type-check`, and `bun run lint`; capture command results and changed-line statistics with `git diff --stat`. <!-- sdd-owner: implementation -->
- [ ] Verify scope boundaries: no catalog/artist changes, no rollback SQL file, no URL/window indexes, no polling/cron/TTL/worker infrastructure, and no unsafe secrets or destructive migration execution. <!-- sdd-owner: implementation -->

### Deferred parent-owned lifecycle actions

- [ ] Before any PR, create or verify a matching issue through the repository issue form/review process and confirm `status:approved`; creating an issue alone does not approve it. Do not publish remotely without authorization. <!-- sdd-owner: parent -->
- [ ] Measure additions + deletions per child PR against 400 changed lines and target about ≤60 minutes of review; make one honest cohesive slicing pass if needed, keeping tests/docs with behavior. If an unavoidable slice still exceeds 400, report its actual count/rationale and stop for maintainer `size:exception` approval; never infer approval or shrink code artificially. <!-- sdd-owner: parent -->
- [ ] When separately authorized, prepare a draft/no-merge tracker PR targeting `dev` (never `main`); child PR #1 targets the tracker branch, and each later child targets the immediately previous branch. Use `.github/PULL_REQUEST_TEMPLATE.md` for tracker and children, link the approved issue, and ensure exactly one `type:*` and one `major|minor|patch` version label per PR. Add chain context and a dependency diagram marking the current child `📍` without replacing the template. No direct commits to `dev`. <!-- sdd-owner: parent -->
- [ ] Verify each child PR diff contains only its current work unit with tests/docs and checks; integrate children in order, keep tracker draft/no-merge until integration, then rebase the final tracker branch onto current `dev` before final review/merge. Record actual branch/PR/label/rebase evidence only if performed. <!-- sdd-owner: parent -->
- [ ] Start or reuse a bounded review after the implementation diff and verification evidence are available, according to the user-owned review switch and provider route; no review begins during this planning edit. <!-- sdd-owner: parent -->
- [ ] Stop and request authorization before any destructive migration rollback/drop of `activity_registration`. <!-- sdd-owner: parent -->
