# Cumulative Apply Progress — Edition Activity Registration

## Git recovery and Work Unit 3 closure

- The original `.git` directory was deleted after B1 implementation. A complete source snapshot was preserved outside the repository (timestamped preservation snapshot). An independent clone of remote `dev` (`bc3746c6`) reconstructed completed WU1, WU2, and WU3-A commits. Their Git trees match the original frozen review trees, but commit IDs changed: WU1 `5b7bf928`, WU2 `6a493515`, WU3-A `469305d7`. Previous review authority/receipts were not assumed transferred to the new Git metadata. The snapshot captured the then-current B1 progress artifact, not the earlier detailed WU1/WU2 log; earlier evidence remains in Engram and checked tasks.
- User explicitly authorized B1's `size:exception` up to 500 lines and its commit. B1 was committed as `e574ef39` (450 additions) on `feat/edition-activity-registration-03b-admin-update`; fresh native high-risk review `review-2fef3a5f7aeeb388` approved and its exact acknowledgement burned authority. Resilience warning `R4-001` was informational only.
- User authorized B2's commit and review. B2 was committed as `d645c631` (99 additions + 155 deletions = 254 changed lines) on `feat/edition-activity-registration-03c-admin-dialog-save`; fresh native high-risk review `review-b3b9e0d78634a5f0` approved and its exact acknowledgement burned authority. Resilience warning `R4-001` was informational only. The branch is clean; all six WU3 tasks are checked and the overall change is 15/40. No push, issue, PR, live migration, full-admin green, or browser interaction verification is claimed.
- The B1/B2 entries below describe their implementation state at the time and predate these commits/reviews. This recovery record supersedes their historical “no commit/review” wording.

## Work Unit 3 — slice B1 update aggregate — completed within approved exception

- Consumed the supplied native `gentle-ai.sdd-status` v2 for `edition-activity-application`: `artifactStore: openspec`, `applyState: ready`, `nextRecommended: apply`, initially 9/40 tasks complete, no dependencies/blockers, repo-local workspace and allowed edit root (the then-current checkout). `actionContext` warnings: none. Strict TDD active (`openspec/config.yaml`, runner `bun run test`). `skill_resolution: paths-injected`; all six injected skills were read. Confirmed the authorized clean branch `feat/edition-activity-registration-03b-admin-update` before source edits.
- Scope was only B1: atomic update action + action tests. User expressly accepted `size:exception` for B1 only, with a hard maximum of 500 authored additions + deletions. Final authored diff was **450 additions, 0 deletions** (194 action + 256 test lines), under the approved ceiling by 50. No dialog or old action was changed. `updateParticipationAction` remained for the exhibition dialog.
- Added `updateActivityAggregateAction` to validate edition participation/activity IDs, verify ownership within one transaction, resolve the authoritative effective activity type (band is forced through the `musica` slug), normalize and convert optional Chile-local registration input, update entity/activity rows, upsert activity detail and complete registration by their unique keys, delete absent registrations (including eligible-to-music changes), and invalidate local/public tags only after commit. Complete registration with effective music type rejects before mutation. Database trigger defense remains in place; the action also explicitly clears the row.
- Added focused action tests for detail/registration upsert, absent config deletion, music rejection/cleanup, band normalization, edition/activity ownership mismatch, commit-only complete cache tags, and injected errors at participation, activity, detail, and registration mutation points. The harness models transaction commit/rollback at the action boundary; it is an injected DB mock, not a live/external database. No live migration or database access was used.
- **Strict TDD evidence (B1):** RED: focused test file first failed 0 pass / 4 failures because the action module did not exist. GREEN: after action implementation and test fixture/import corrections, focused file passed 5/5, 59 assertions. TRIANGULATE: participation/admin scoped suite passed 49/49, 245 assertions; each injected late failure kept commit marker false and skipped invalidation; success verified all required admin/public tags after commit. REFACTOR: Prettier, admin type-check and lint passed; corrected test/typing issues and removed unused import.
- **Verification (B1):** focused aggregate tests **5/5** (59 assertions); participation suite **49/49** (245 assertions); `bun run type-check --filter=@frijolmagico/admin`, `bun run lint --filter=@frijolmagico/admin`, and `git diff --check` passed at B1 completion. Full admin suite was not run. Older progress records pre-existing catalog/avatar failures and two `next/cache` load errors; not re-investigated or claimed fixed.
- **Files changed (B1):** new `apps/admin/src/app/(core)/eventos/participaciones/_actions/activities/update-activity-aggregate.action.ts` and `apps/admin/tests/unit/app/(core)/eventos/participaciones/_actions/update-activity-aggregate.test.ts`, plus tasks/apply-progress updates. No legacy action/dialog/exhibition caller changed.
- **Rollback boundary (B1):** remove the new aggregate action and test to revert B1; B2 dialog submission, WU1/WU2, and other callers remain independent. Delivery boundary: accepted B1 exception only, 450/500 lines; no commit/push/issue/PR/review/receipt/live migration. Historical environment note: final cleanup during B1 reported `.git` metadata missing; no recovery/reinitialization was attempted. B2 verified its own clean branch/worktree at start.
- **Persisted tasks at B1 completion:** 14/40 checked; each B1 task row was visibly `[x]`; parent-owned lifecycle rows remained unchanged.

## Work Unit 3 — slice B2 update dialog aggregate submission — completed

- Consumed the supplied native `gentle-ai.sdd-status` v2 for `edition-activity-application`: `artifactStore: openspec`, `applyState: ready`, `nextRecommended: apply`, repo-local workspace and allowed edit root (the then-current checkout), initially 14/40 tasks complete, no native blockers, and `actionContext.mode: repo-local`. Strict TDD is active (`openspec/config.yaml`, runner `bun run test`). `skill_resolution: paths-injected`; all six injected skills were read. Verified the working tree was clean on `feat/edition-activity-registration-03c-admin-dialog-save` before changes.
- Workload guard: the forecast says chained PR recommended and budget risk High. Parent assigned this concrete WU3 B2 work-unit slice on the current chain branch, so this implementation is limited to this slice. No `size:exception`; authored source/test diff remains within 400 lines. No WU4 files (read model, types, composer, form registration fields/defaults/presentation) were changed.
- Replaced only `UpdateActivityDialog`'s sequential `executeUpdatePlan`/separate participation, activity, and detail actions with one call to the reviewed `updateActivityAggregateAction`, passing current edition, participation, activity, and detail values. Preserved the existing form, accessibility markup, pending/disabled behavior, error/success feedback, form reset, dialog close, and destructive footer behavior. Left `updateParticipationAction` intact and verified the exhibition dialog remains its caller. No other legacy caller was removed.
- Updated the now-obsolete detail-persistence source contracts to assert aggregate submission and unconditional detail values; added focused dialog-save contract tests covering a single aggregate payload, removed per-record save calls, retained feedback/reset/close behavior, and exhibition caller preservation.
- **Strict TDD evidence (B2):**

| Behavior | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|
| Dialog uses a single aggregate save and preserves UX/caller boundaries | Added `update-activity-dialog.contract.test.ts` first; focused run failed (2 failures) because the dialog had no aggregate action and prior success/error checks did not yet match the intended result shape. | Replaced the sequential plan with the aggregate action call. Focused dialog + aggregate-action tests passed (8 tests total, 73 assertions); action harness confirmed transaction contract still works. | First participations-scope run surfaced four stale expectations in the pre-existing detail persistence contract, which asserted the now-retired branch-specific actions. Updated those tests to assert the new aggregate contract. Reran participations suite successfully: **50/50 tests, 255 assertions**. Admin type-check and lint passed. | Prettier formatted affected tests. Kept changes scoped to dialog submission and contract coverage; did not alter form fields/defaults or other callers. Final `git diff --check` passed. |

- **Verification:** `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones'` — **50/50 tests, 255 assertions**; `bun run type-check --filter=@frijolmagico/admin` — passed; `bun run lint --filter=@frijolmagico/admin` — passed. Runtime browser harness: **N/A**; this dialog's submission is covered by focused source contract tests plus the existing aggregate action transaction harness, and the initialized project config reports no Playwright/E2E setup. No live database/migration was run.
- **Files changed by B2:** `apps/admin/src/app/(core)/eventos/participaciones/_components/update-activity-dialog.tsx`; `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/update-activity-dialog.contract.test.ts` (new); `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/update-activity-dialog.contracts.test.ts` (revised obsolete save-path assertions); this progress file; `openspec/changes/edition-activity-application/tasks.md` (one WU3 implementation checkbox completed).
- **Persisted task state:** WU3 task rows are now complete across slices A, B1, and B2. Re-read `tasks.md` before return to confirm the B2 `GREEN: Remove or migrate...` row is visibly `[x]`. Overall task state advances from 14/40 to 15/40; no WU4/other work-unit or parent-owned rows were checked.
- **Review budget / chain boundary:** B2 only, bounded to one dialog save-path migration and its focused tests; authored source/test changes must remain <=400 additions + deletions. No commit, push, issue, PR, review/receipt, release/delivery gate, or live migration was performed.
- **Rollback boundary:** revert the dialog's `updateActivityAggregateAction` import/call and restore the three legacy action imports plus sequential save plan, and revert the two focused contract test files to their previous expectations. The aggregate action, WU1/WU2, WU3 A/B1, exhibition dialog and `updateParticipationAction`, and database migration are outside this slice's rollback boundary.

## Work Unit 4 — admin read model and dialogs (partial verification)

- Consumed native `gentle-ai.sdd-status` v2 supplied by parent: `edition-activity-application`, OpenSpec, 15/40 complete, `applyState: ready`, `nextRecommended: apply`, no blockers or notes; repo-local workspace `/home/strocs/dev/FrijolMagico-edition-activity` and identical allowed edit root. `actionContext` warnings: none. Confirmed clean `feat/edition-activity-registration-04-admin-read-dialog` before changes. `skill_resolution: paths-injected` (five supplied paths read). Strict TDD, `bun run test`. Installed dependencies with `bun install --frozen-lockfile` after initial test command failed because `turbo` was absent; lockfile unchanged.
- Workload: approved feature-branch-chain, WU4 only, no publication or commit. Source/test additions + deletions: **252** (including two new files), excluding tasks/progress. Cohesive boundary: admin read/form flow; WU5 public data untouched. No size exception claimed.
- Source: admin DAL left joins optional registration and converts canonical UTC values on the server to Chile-local defaults; composer carries null/complete registration to update dialog. Shared accessible registration controls and clear helper serve both create/update; music selection or band clears and revalidates, hides fields. Both actions receive the form registration object; successful saves reset/close and refresh. Server-side actions still reject forged music registration (existing action tests). No persistence action changed.

### TDD Cycle Evidence (WU4)

| Cycle | Evidence |
|---|---|
| RED | Added admin read/dialog source-contract tests first; initial focused run failed before dependencies (`turbo` unavailable); after frozen install, 0/3 passed with missing join, fields, and music clear. |
| GREEN | Added DAL join/defaults, composer type/propagation, shared inputs and dialog wiring. Focused tests 3/3, 32 assertions; admin type-check passed after correcting nullable join mapping. |
| TRIANGULATE | Participation suite 53/53 (287 assertions); full admin suite 504/504 (1323 assertions); `bun run type-check --filter=@frijolmagico/admin` and `bun run lint --filter=@frijolmagico/admin` passed. Inspected JSX/field labels, defaults and reset/refresh source; no rendered/browser form check was performed. |
| REFACTOR | Shared one field group/clear helper across both dialogs; Prettier formatted files; kept existing aggregate actions and form semantics. |

- **Persisted tasks:** GREEN and REFACTOR checked in `tasks.md` (17/40 total). RED remains unchecked: new tests are source-contract tests, not runtime rendered component or executable DAL-mapping tests. TRIANGULATE remains unchecked: no rendered form/manual browser contract inspection. These two WU4 verification gaps need a follow-up WU4 test/inspection slice before advancing to WU5. No other task is claimed done.
- **Changed paths:** `apps/admin/src/app/(core)/eventos/participaciones/_components/{activity-registration-fields,create-activity-dialog,update-activity-dialog}.tsx`, `_lib/data-access-layer/get-activities-with-details.ts`, `_lib/participation-composer.ts`, `_types/activity.types.ts`, `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.contract.test.ts`, and this change's tasks/apply-progress files.
- **Deviation/risk:** runtime component and DAL join-null mapping tests remain absent; source-contract tests alone do not prove rendered behavior. No browser/E2E setup exists in project config. No WU5 work, PR, issue, migration, secrets or destructive action performed.

### WU4-A — executable admin DAL read-model coverage (current launch)

- Parent supplied valid native `gentle-ai.sdd-status` v2: `applyState: ready`, `nextRecommended: apply`, OpenSpec, 17/40 complete, no blockers, `actionContext.mode: repo-local`, workspace and allowed edit root `/home/strocs/dev/FrijolMagico-edition-activity`; no action-context warnings. Session preflight auto/OpenSpec/ask-on-risk/400 was supplied, and the user explicitly resolved this high-risk slice as a feature-branch-chain split (WU4-A then WU4-B). `skill_resolution: paths-injected` (chained-pr and vitest). Strict TDD guidance read from global support; no project override exists. Existing uncommitted WU4 work preserved; no WU5 edits.
- Executed the DAL and composer with a mocked read query (not a live DB): asserted the actual two left joins, missing row null through composer, Chile winter/summer local defaults, and canonical UTC round trip. A missing registration ID with stray malformed timestamp previously raised during conversion; conversion now runs only for a present registration ID. The real SQL left-join yields all-null registration columns when absent; the stray-column case is a defensive regression test, not a claimed live SQL fixture.

#### TDD Cycle Evidence (WU4-A)

| Scope | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| Executable DAL read + composer (`_lib/get-activities-with-details.test.ts`) | Existing dialog source contract 3/3 passed before DAL edit | Wrote executable test; 1 pass, 1 failure: absent ID with stray timestamp threw `La fecha UTC no es válida` | Guarded UTC conversion on registration ID; focused 2/2, 11 assertions | Added ordinary all-null left-join case and UTC→Chile→UTC round trip across winter/summer; focused 3/3, 17 assertions | Prettier formatted the test; focused 3/3 remained green; no other source refactor |

- Verification: `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones/_lib/get-activities-with-details.test.ts'` 3/3; participations scope before triangulation 55/55 (298 assertions); full admin suite after triangulation 507/507 (1340 assertions). `bun run type-check --filter=@frijolmagico/admin --force`, `bun run lint --filter=@frijolmagico/admin --force`, and `git diff --check` passed. Tests: three new executable cases; no browser/rendered dialog or live database verification claimed.
- Changed in this launch: `apps/admin/tests/unit/app/(core)/eventos/participaciones/_lib/get-activities-with-details.test.ts` (new); `apps/admin/src/app/(core)/eventos/participaciones/_lib/data-access-layer/get-activities-with-details.ts` (small conditional conversion fix); this progress artifact. Existing WU4 dialog source/test edits retained unchanged.
- **Measured prospective source/test slices (additions + deletions vs current Git HEAD, counting untracked files by physical line count; excludes cumulative OpenSpec planning artifact):** WU4-A DAL/composer/types plus executable DAL test: DAL 35+3, composer 2+1, types 2+1, test 142 = **186**. WU4-B dialog presentation/shared fields plus source-contract test: create dialog 31+1, update dialog 31+3, shared fields 84, source-contract test 60 = **210**. Both are independently under 400 at this checkpoint; WU4-B still requires rendered interaction tests and must be remeasured before delivery, without trimming coverage. WU4-A depends on WU3; WU4-B depends on WU4-A. Feature-branch-chain tracker (future authorized publication only) → earlier children → WU4-A 📍 → WU4-B → WU5; no branches/commits/PRs created by this launch. WU4-A rollback is DAL/composer/types plus executable test; WU4-B rollback is dialog/shared-fields/source-contract test; existing uncommitted work remains in place pending parent slicing.
- **Persisted task checkboxes:** no changes; GREEN/REFACTOR WU4 remain `[x]` from prior launch. Whole-WU4 RED and TRIANGULATE remain unchecked until rendered dialogs, music clearing, submission, defaults and refresh are exercised. Remaining exact lines:
  - [ ] RED: Add read-model and component tests for empty/update Chile-local defaults, registration join absence, music hiding/clearing, complete submission wiring, and forged payload coverage at the action boundary. <!-- sdd-owner: implementation -->
  - [ ] TRIANGULATE: Run admin tests and manually inspect the rendered form contract for create/update, canonical round trips, reset/refresh behavior, and no client-only eligibility trust. <!-- sdd-owner: implementation -->
- **Deviation / risk:** WU4-A mocks the query chain and does not execute SQL against a database; a rendered create/update contract remains unverified. No task completion or readiness for verify claimed. All WU5–WU7 implementation and parent-owned lifecycle lines remain unchecked as listed below.

### Gatekeeper follow-up — WU4 verification boundary

- Re-read WU4 tasks and current diff; no new source or test edits in this follow-up. Native v2 status from parent remains `apply`/ready, OpenSpec, repo-local allowed root, 17/40 checked, no blockers or action-context warnings. The checkout already contains the previous WU4 partial changes; it was not clean at this follow-up start. No fresh native status was produced.
- Whole uncommitted WU4 diff before this note: **277 additions + deletions**, including **252 source/test** (two untracked files counted by physical lines) and **25 OpenSpec**. Only 123 lines remain under the 400-line whole-work-unit ceiling. One honest slicing assessment: executable DAL join/null test plus rendered create/update dialog defaults, music interaction/clearing, payload/refresh, and canonical round-trip tests need a database mock and a React interaction/render harness; the repository has SSR tests but no DOM interaction runner. Building those tests honestly with their fixtures/harness and evidence would exceed the remaining 123 lines; a tests-only PR would detach proof from the source behavior it verifies. No `size:exception` is authorized. Stop rather than claim completion or trim tests.
- Rechecks: `bun run test --filter=@frijolmagico/admin` passed **504/504**, 1323 assertions (source contracts, not missing runtime coverage). `bun run type-check --filter=@frijolmagico/admin --force` passed uncached; `bun run lint --filter=@frijolmagico/admin --force` passed uncached. Earlier cached invocations also passed. No rendered-form inspection was performed. RED and TRIANGULATE remain unchecked; GREEN and REFACTOR retain their earlier evidence. Proposed boundary is a further cohesive WU4 verification slice with the implementation, requiring a new budget/delivery decision or explicit maintainer `size:exception`, not WU5.

### WU4-B rendered-verification attempt — blocked, no completion claimed

- Consumed parent-supplied native `gentle-ai.sdd-status` v2 for `edition-activity-application`: OpenSpec, 17/40, `applyState: ready`, `nextRecommended: apply`, dependencies ready, empty blockers, repo-local workspace and allowed root `/home/strocs/dev/FrijolMagico-edition-activity`; no action-context warnings. Session preflight auto/OpenSpec/ask-on-risk/400 and explicit feature-branch-chain WU4-B assignment were present. `skill_resolution: paths-injected` (four injected skills read). Strict-TDD global support read; no project-local override.
- Existing WU4-A and WU4-B worktree edits were preserved. Tried a focused React DOM interaction test using the already locked `happy-dom` and `@testing-library/react` packages, temporarily adding admin development dependencies. The attempted test hung during React root rendering under Bun after component import, both with and without `act`; focused `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx'` timed out repeatedly (15–120 seconds), with no assertion result. This is an infrastructure RED, **not** a failing behavioral test. No GREEN, TRIANGULATE, REFACTOR, or rendered/manual interaction success is claimed. The incomplete test and temporary dependency/lockfile changes were removed; the original worktree code and source-contract tests remain untouched. No new production code was written.
- `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones'` passed 56/56 (304 assertions). `bun run lint --filter=@frijolmagico/admin --force` and `git diff --check` passed. `bun run type-check --filter=@frijolmagico/admin --force` **failed** in the pre-existing uncommitted WU4-A test `_lib/get-activities-with-details.test.ts:137`: `unknown` is not assignable to `string` for `startAt`/`endAt`. That WU4-A file was not edited in this attempt. No full-admin test or rendered dialog pass is claimed.
- Review boundary: WU4-B remains 210 source/test changed lines (including its untracked files) versus HEAD; WU4-A remains 186. No new WU4-B source/test diff was retained, no `size:exception` requested, no commit/PR/publication/migration/WU5. Remaining WU4 RED and TRIANGULATE lines are unchanged immediately below; checkboxes remain 17/40. A reliable rendered-interaction harness and the WU4-A type-check failure require follow-up before marking either verification task done.

### Gatekeeper follow-up — WU4-A test type-check repair

- User-authorized scope was only the existing WU4-A DAL test assertion; no dialog harness, dependency, production source, WU5, branch, commit, or PR change. The prior parent-supplied native v2 apply status (OpenSpec, 17/40, repo-local allowed workspace, no blockers/action warnings) and the current session preflight were consumed. `skill_resolution: fallback-path` (known Vitest skill path; no new skill discovery). Strict TDD remains active. This note supersedes the type-check failure above, not the rendered-verification gap.
- **TDD Cycle Evidence:** RED — prior uncached admin type-check failed at the existing test line 137 because `rows: Record<string, unknown>[]` made both expected instants `unknown`; safety-net focused runtime test passed 3/3, 17 assertions before edit. GREEN — replaced only the expected `startAt`/`endAt` values with the concrete canonical UTC fixture literals; focused test passed 3/3, 17 assertions and `bun run type-check --filter=@frijolmagico/admin --force` passed uncached. TRIANGULATE — the existing winter/summer round-trip assertion and absent-row cases still passed in the focused suite. REFACTOR — no further refactor needed; `git diff --check` passed.
- WU4-A source/test slice remains **186** authored changed lines: the test is still 142 physical lines, and the edit replaced two lines with two lines. WU4-B remains **210** authored changed lines; both slices remain below 400 excluding cumulative planning artifacts. `tasks.md` remains 17/40; WU4 RED and TRIANGULATE stay unchecked until actual rendered-interaction evidence exists.

### WU4-B scoped rendered-verification recovery — partial, review exception needed

- Consumed parent native `gentle-ai.sdd-status` v2: OpenSpec `edition-activity-application`, 17/40, `applyState: ready`, `nextRecommended: apply`, `dependencies.apply: ready`, no blockers; `actionContext.mode: repo-local`, workspace and allowed edit root `/home/strocs/dev/FrijolMagico-edition-activity`, no warnings. Session preflight auto/OpenSpec/ask-on-risk/400; parent assigned only WU4-B on the approved feature-branch-chain. `skill_resolution: paths-injected` (chained-pr, vitest, no-use-effect). Global strict-TDD guidance read; no local override.
- Diagnosed the previous import/render timeout in bounded stages: a trivial `createRoot`/`act` root rendered in ~20 ms, shared `ActivityRegistrationFields` with real React Hook Form rendered in ~85 ms, a mocked `EntityFormDialog` shell rendered in ~6 ms, and the actual create/update dialogs rendered under mocked shell/store/select/actions without a hang. Admin has no DOM preload; this test installs `happy-dom` globals explicitly before dynamic component imports, using the existing root dependency without changing the lockfile. Diagnostic-only root/shell smoke cases were removed after isolation; retained cases assert production behavior.
- Added `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx`: executable shared/create empty defaults, update Chile-local defaults, music hide then clear verified on return to eligible type, and complete update aggregate payload with success close/refresh. Existing WU4-A DAL and action tests cover absent join, canonical round trips and forged music payload. The shell/select/store are test doubles; this is not a browser or live database check. Create submission and update reset after success are not directly asserted by this rendered test, nor is band selection interaction.

#### TDD Cycle Evidence (WU4-B recovery)

| Scope | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| Rendered dialog recovery | Existing source-contract focused 3/3 passed | Initial create-dialog assertion failed 1/4 because test incorrectly selected IDs rather than registered input names; update music assertion failed 1/5 because the double selected type ID 2, not music ID 3. These were test-harness errors, **not** behavioral RED. Earlier 120s timeout was infrastructure, not RED. | After correcting selectors/type double: focused 5/5, then 6/6 including update submission; no production edit needed. | Switched to music and back to verify cleared values; submitted complete update and asserted action payload plus close/refresh. | Formatted with Prettier; removed diagnostic-only root/shell smoke tests (not behavioral coverage). Focused retained cases 4/4, 12 assertions. |

- Verification: focused render test **4/4, 12 assertions**; participation scope with diagnostics still present **62/62, 318 assertions**; uncached admin type-check and lint passed before diagnostic removal (removal only changed test code); `git diff --check` passed afterward. No full-admin run or fresh native status was produced in this recovery. Prior WU4-A type-check fix was preserved.
- Review budget: prior WU4-B source/contract slice 210 authored additions+deletions; rendered test adds **294** physical lines, totaling **504** source/test lines before progress, 104 over the 400-line budget. This is one cohesive form presentation plus its interaction evidence; no comments/tests/docs were compressed to fit. **Maintainer `size:exception` is recommended but not approved**. The previously suggested 210-line boundary is superseded. Do not commit/publish this over-budget slice without an explicit decision. WU4-A remains separate at 186 lines; no WU5 edits, commit, PR or push.
- Persisted tasks: **no checkbox changed**; 17/40 remain checked. Whole-WU4 RED remains unchecked pending create submission/band interaction and a review decision on the oversized cohesive slice. TRIANGULATE remains unchecked pending rendered reset semantics and direct form inspection beyond mocked shell, plus review decision. Exact unchecked lines remain below. No `Ready for verify` claim.

### WU4-B completed rendered interaction coverage — approved size exception

- Consumed parent native `gentle-ai.sdd-status` v2: OpenSpec, 17/40, apply ready/recommended, no blockers; repo-local workspace and allowed root `/home/strocs/dev/FrijolMagico-edition-activity`, no action-context warnings. Current preflight auto/OpenSpec/ask-on-risk/400; human explicitly accepted `size:exception` for WU4-B only on feature-branch-chain. Injected Vitest/no-use-effect skills read; injected chained-pr path was malformed, so the known supplied registry location was loaded as `fallback-path`. Global strict-TDD guidance read; no local override. No production source edits in this launch.
- **TDD Cycle Evidence:** Safety net existing rendered test 4/4, 12 assertions. RED: wrote executable create complete-registration test; focused 4/5 failed because the harness clicked the wrong select, then lacked `HTMLInputElement`, then submitted five empty values with React DOM initialized before the happy-dom globals; these are harness REDs, not production defects. GREEN: corrected the combobox double/selector and installed globals before dynamically importing `react-dom/client`; focused 5/5, 15 assertions, actual entered values passed to create action and reset to empty. TRIANGULATE: added band selection and return path, update reset/disabled after success, and inspected rendered label/input type/default markup; first band return still selected music, so explicitly returned to eligible activity type; focused 6/6, 26 assertions. REFACTOR: Prettier formatted test; focused 6/6, 26 assertions after formatting. No production change needed. Existing WU4-A DAL tests cover absent join and Chile winter/summer UTC round trips; WU3 action tests cover forged music payload rejection. These are tests against real dialogs and form controls with mocked shell/select/store/actions, not a browser or live DB.
- **Verification:** `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx'` 6/6 (26 assertions); `bun run test --filter=@frijolmagico/admin` 513/513 (1366 assertions); `bun run type-check --filter=@frijolmagico/admin --force`, `bun run lint --filter=@frijolmagico/admin --force`, `git diff --check` passed. Rendered markup checked: five labeled inputs (`url`, date, time) with empty create defaults, Chile-local update defaults, music/band hidden fields, edit/submit/reset/refresh; real dialog shell focus, keyboard/browser behavior, real selects, and actual database integration remain untested. Form eligibility remains presentation-only; server action tests reject forged music data.
- **Persisted tasks:** WU4 RED and TRIANGULATE switched to `[x]` after focused and full checks; WU4 GREEN/REFACTOR remained checked. Overall 19/40; WU5 and beyond not touched. Files changed this launch: `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx`, this progress and `tasks.md`. No commit, push, issue, PR, or publication.
- **Measured authored source/test diffs vs HEAD** (tracked additions + deletions; untracked files by physical lines; excludes cumulative planning artifacts): WU4-A DAL/composer/types and executable DAL test **186** (unchanged); WU4-B create 32, update 34, shared fields 84, source-contract test 60, rendered test 471 = **681** additions + deletions, **281 above 400**. Previous WU4-B checkpoint 504 is superseded; 177 additional rendered-test lines supplied complete create, band, reset and markup evidence. Approved exception applies to this cohesive WU4-B slice only, not WU5 or delivery/publication. No trimming of comments/tests/docs to meet budget. Feature-branch-chain future boundary: WU4-A → WU4-B 📍 → WU5, with tracker/earlier children preceding; WU4-B rollback is dialogs/shared fields and their two tests, WU4-A stays independent.

### WU4-B independent-verifier coverage closure — UI submission after music/band selection

- Consumed parent-provided native `gentle-ai.sdd-status` v2: OpenSpec `edition-activity-application`, `applyState: ready`, `nextRecommended: apply`, 19/40 checked, no blockers, repo-local workspace and allowed edit root `/home/strocs/dev/FrijolMagico-edition-activity`, no `actionContext` warnings. Preflight auto/OpenSpec/ask-on-risk/400; explicit `size:exception` acceptance applies to WU4-B only on the feature-branch-chain. `skill_resolution: paths-injected` (chained-pr, Vitest); strict-TDD global support read, no local override. No native status read tool was available after edits; no fresh recommendation is claimed.
- Independent verifier identified the missing rendered UI submission proof: the earlier WU4-B test hid and cleared registration on music/band selection but did not submit while music/band remained selected. Extended **only** `apps/admin/tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx` to submit the update with music selected and the create form with band selected, asserting each action receives the five-field absent registration payload and effective music type. Existing assertions for empty/Chile-local defaults, visibility, clearing after return to eligible type, complete submissions, reset, close and refresh remain. No production change was required.

#### TDD Cycle Evidence (WU4-B coverage closure)

| Task / layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| Rendered UI music/band submission; mocked-shell integration | Existing focused test: 6/6, 26 assertions | Added band submit assertion before returning to eligible type; first run failed 5/6 because successful create reset the form to the band/music defaults and the old return-to-eligible assertion saw no registration fields. This was a test-order/harness failure, **not** a production defect or proof that absent registration was wrong. | Moved the submission after the old return-to-eligible/clear assertions and selected band again; focused 6/6, 27 assertions, absent payload confirmed. No production edit. | Added distinct update-to-music submission before returning to eligible type; focused 6/6, 28 assertions, both action paths receive absent values with music type. | Prettier check passed with no formatting edit; focused 6/6 remained green after the second path. |

- Verification: `bun run test --filter=@frijolmagico/admin -- 'tests/unit/app/(core)/eventos/participaciones/_components/activity-registration-dialog.render.test.tsx'` **6/6, 28 assertions**; `bun run test --filter=@frijolmagico/admin` **513/513, 1368 assertions**; `bun run type-check --filter=@frijolmagico/admin --force`, `bun run lint --filter=@frijolmagico/admin --force`, `bunx prettier --check` on the rendered test, and `git diff --check` all passed. No live DB, real select/dialog shell, keyboard/browser or E2E verification: happy-dom executes real forms/dialog components but mocks shell, select, store and actions. Those limitations remain for a later browser check; no browser success is claimed.
- Persisted tasks: WU4 RED/GREEN/TRIANGULATE/REFACTOR were already `[x]`, now backed by the additional submission assertions; **no new checkbox was changed**. Re-read tasks after this note: 19/40 checked, WU5–WU7 implementation unchecked. No WU5, commit, push, PR, live migration, or source change. Deviation from design: none for this test-only closure.
- Review boundary: WU4-B only, source/test authored changed lines vs HEAD (tracked additions + deletions; untracked files by physical lines): create dialog 32 + update dialog 34 + shared fields 84 + source-contract test 60 + rendered test **517** = **727**, **327 over 400**. This supersedes the earlier 681/281 figure; the added 46 lines retain coverage rather than compressing tests. Exception accepted for WU4-B only; WU4-A remains separate (186). Future feature-branch-chain: tracker → earlier children → WU4-A → WU4-B 📍 → WU5; no publication performed. Rollback boundary remains WU4-B dialog/shared fields plus both tests.

### WU4 work-unit closure

- After independent pre-commit verification, WU4-A was committed as `a6f09022` (`feat(admin): read activity registration defaults`): DAL, composer, types, and executable DAL tests in one 186-line source/test boundary. The WU4-B commit carrying this closure records the dialogs, shared controls, component tests, and task/progress artifacts together under the approved WU4-B-only `size:exception` (727 source/test lines). These are local commits on `feat/edition-activity-registration-04-admin-read-dialog`; no push, PR, or merge is claimed.
- Final independent pre-commit checks on the combined worktree: `bun run test --filter=@frijolmagico/admin` **513/513, 1368 assertions**; `bun run type-check --filter=@frijolmagico/admin --force`, `bun run lint --filter=@frijolmagico/admin --force`, and `git diff --check` passed. Rendered tests use mocked shell/select/store/actions; browser interaction and live database integration remain unverified. WU4 is 19/40 overall; WU5 is the next implementation boundary, not part of either commit.

### Remaining exact unchecked implementation task lines
- [ ] RED: Add tests for the exact left join, nested nullable registration object, absence of current-time SQL predicates, mapper/DTO propagation, missing-row `null`, and music-path exclusion. <!-- sdd-owner: implementation -->
- [ ] GREEN: Add the left join and registration JSON selection, extend public types/mappers/fixtures, and preserve `MusicActivityItem` routing without computing active state server-side. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run the scoped web tests and inspect generated/query fixtures to verify inactive registrations remain serialized and no browser database request is required. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Normalize nullable mapping and fixture helpers; avoid broad DTO or catalog/artist changes. <!-- sdd-owner: implementation -->
- [ ] RED: Test semantic anchor CTA variants, preserved top-bar/timeline labels and placement, no button nesting, visible keyboard focus, and readable default/hover/focus contrast; test reusable Badge as a non-interactive `span` accepting content without a tab stop. <!-- sdd-owner: implementation -->
- [ ] GREEN: Extract a reusable web link-CTA primitive with solid top-bar and offset-border timeline variants, reuse at both existing links, and add a reusable brand-aligned non-interactive web Badge; leave native `Button`, text-link `LinkBtn`, and admin Badge unchanged. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run scoped web tests and inspect focus/contrast and narrow visual parity for both existing links and the Badge; do not import admin Badge or use hardcoded `NewBadget`. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Share only navigation CTA visuals and retain existing web link semantics, labels, and layout without broad redesign. <!-- sdd-owner: implementation -->
- [ ] RED: Add pure helper and component tests for hidden server/first render, inclusive start/end, inactive/malformed values, boundary timers, timeout re-evaluation, focus/visibility reconciliation, cleanup, collapsed `Inscríbete` Badge, expanded-only `Inscríbete Aquí` anchor CTA, music exclusion, exact labels, target/rel, no button nesting, readable contrast, and focus-visible semantics. <!-- sdd-owner: implementation -->
- [ ] GREEN: Implement client-only leaf affordances with `active=false` initialization, boundary scheduling without polling, lifecycle listeners, safe anchor attributes through the shared link-CTA, reusable non-interactive web Badge at top-right, and CTA at the bottom of expanded content. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: Run web tests with fake clocks/timers and explicit focus/visibility events; verify server-rendered markup contains neither label and no CTA appears in summary/minimal cards. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: Keep `ActivityItem` server-first, isolate timing logic from markup, and preserve existing card/details semantics and music rendering. <!-- sdd-owner: implementation -->
- [ ] Run the complete strict-TDD evidence pass: database, admin, and web RED/GREEN/TRIANGULATE/REFACTOR results are recorded in the apply/verify artifacts, with failures resolved before delivery. <!-- sdd-owner: implementation -->
- [ ] Run `bun run test`, `bun run type-check`, and `bun run lint`; capture command results and changed-line statistics with `git diff --stat`. <!-- sdd-owner: implementation -->
- [ ] Verify scope boundaries: no catalog/artist changes, no rollback SQL file, no URL/window indexes, no polling/cron/TTL/worker infrastructure, and no unsafe secrets or destructive migration execution. <!-- sdd-owner: implementation -->

### Deferred parent-owned lifecycle actions

- [ ] Before any PR, create or verify a matching issue through the repository issue form/review process and confirm `status:approved`; creating an issue alone does not approve it. Do not publish remotely without authorization. <!-- sdd-owner: parent -->
- [ ] Measure additions + deletions per child PR against 400 changed lines and target about ≤60 minutes of review; make one honest cohesive slicing pass if needed, keeping tests/docs with behavior. If an unavoidable slice still exceeds 400, report its actual count/rationale and stop for maintainer `size:exception` approval; never infer approval or shrink code artificially. <!-- sdd-owner: parent -->
- [ ] When separately authorized, prepare a draft/no-merge tracker PR targeting `dev` (never `main`); child PR #1 targets the tracker branch, and each later child targets the immediately previous branch. Use `.github/PULL_REQUEST_TEMPLATE.md` for tracker and children, link the approved issue, and ensure exactly one `type:*` and one `major|minor|patch` version label per PR. Add chain context and a dependency diagram marking the current child `📍` without replacing the template. No direct commits to `dev`. <!-- sdd-owner: parent -->
- [ ] Verify each child PR diff contains only its current work unit with tests/docs and checks; integrate children in order, keep tracker draft/no-merge until integration, then rebase the final tracker branch onto current `dev` before final review/merge. Record actual branch/PR/label/rebase evidence only if performed. <!-- sdd-owner: parent -->
- [ ] Start or reuse a bounded review after the implementation diff and verification evidence are available, according to the user-owned review switch and provider route; no review begins during this planning edit. <!-- sdd-owner: parent -->
- [ ] Stop and request authorization before any destructive migration rollback/drop of `activity_registration`. <!-- sdd-owner: parent -->
