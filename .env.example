# =============================================================================
# FRIJOL MAGICO - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in values
# =============================================================================

# -----------------------------------------------------------------------------
# Data Source Configuration (Development Only)
# -----------------------------------------------------------------------------
# Controls data source override for development.
# Options: 'real' | 'local'
#
# - Not set (default): Uses intelligent defaults:
#   * Modules with prod='cms' → use 'mock' in development
#   * Modules with prod='database' → use 'local' (file:local.db) in development
#
# - DATA_SOURCE=real: Force all modules to use their production data source
# - DATA_SOURCE=local: Force use of intelligent defaults (same as not set)
#
# Note: In production/preview (VERCEL_ENV), this variable is ignored.
# Each module always uses its configured prod source (cms or database).
#
# Local DB Setup (for prod='database' modules):
# The local database is read from a SQLite file specified in TURSO_DATABASE_URL
# using the file: protocol (e.g., file:local.db). The libSQL client will
# directly access this file without needing a separate server process.
#
# DATA_SOURCE=local

# -----------------------------------------------------------------------------
# Turso Database (used by application)
# -----------------------------------------------------------------------------
# For development with local database, use file: protocol:
# TURSO_DATABASE_URL=file:local.db
#
# For production or remote development, use https:// or libsql://:
TURSO_DATABASE_URL=https://your-database-org.turso.io
TURSO_AUTH_TOKEN=your-auth-token
TURSO_DATABASE_NAME=your-database-name

# -----------------------------------------------------------------------------
# Geni Migration Tool
# -----------------------------------------------------------------------------
# Geni uses these variables for database migrations
# DATABASE_URL must use https:// protocol for Turso/libSQL
DATABASE_URL=https://your-database-org.turso.io
DATABASE_TOKEN=your-auth-token
DATABASE_MIGRATIONS_FOLDER=./db/migrations
DATABASE_SCHEMA_FILE=./db/schema.sql

# -----------------------------------------------------------------------------
# Google Sheets CMS
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=your-api-key
CATALOG_SHEET_ID=your-spreadsheet-id
APPROVED_ARTISTS_SHEET_ID=your-spreadsheet-id
FESTIVAL_2025_SCHEDULE_SHEET_ID=your-spreadsheet-id

# -----------------------------------------------------------------------------
# Google Analytics
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXX

